name: Deploy to Workshop

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    if: github.repository == 'ACF-Team/imagestickers'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          path: project

      - name: Upload to Workshop
        uses: CFC-Servers/gmod-upload@master
        with:
          id: 3025226322
          changelog: "${{ github.event.head_commit.message }}"
          title: "Image Stickers - Internet pictures attached to entities"
          type: "entity"
          tag1: "build"
          tag2: "fun"
        env:
          STEAM_USERNAME: ${{ secrets.ACF_STEAM_ACCOUNT }}
          STEAM_PASSWORD: ${{ secrets.ACF_STEAM_PASSWORD }}
